import React, {useState} from 'react';

const TrainingTable = () => {

    return (
        <>
            <!-- Weeks graph -->
            <!-- Filter & Sorts -->
            <!-- Trainings -->
        </>
    );
};

export default TrainingTable;