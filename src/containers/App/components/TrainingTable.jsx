import React, {useState} from 'react';

const TrainingTable = () => {

    return (
        <>
            <!-- Weeks graph -->
            <!-- Filters -->
            <!-- Trainings -->
        </>
    );
};

export default TrainingTable;